server {
    listen {{ script_port }};
    server_name {{ ansible_host }};

    location / {
        include proxy_params;
        proxy_pass http://unix:{{ script_path }}/{{ script_name }}.sock;
    }
}
